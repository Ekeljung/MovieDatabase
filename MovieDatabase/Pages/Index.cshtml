@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<!-- Page Content -->
<div class="container">

    <div class="row">

        <!-- Blog Entries Column -->
        <div class="col-md-8">

            <h1 class="my-4">
                Filmdatabas
                <small>Högst rankade filmerna</small>
            </h1>

            <!-- Blog Post 000 -->
            @foreach (var image in Model.Images)
            {
                <div class="card mb-4">
                    <img class="card-img-top" src="~/img/@image.MovieImg" alt="Card image cap">
                    <div class="card-body">
                        <h2 class="card-title">@image.Title</h2>
                        <p class="card-text">@image.Description</p>
                        <a href="@image.MovieURL" class="btn btn-primary">Läs mer &rarr;</a>
                    </div>
                    <div class="card-footer text-muted">
                        Kategori:
                        <a href="?Category=@image.Category">@image.Category</a>
                    </div>
                </div>
            }

            <!-- Blog Post 1 -->
            <!--<div class="card mb-4">
                <img class="card-img-top" src="~/img/@Model.AllImages.ToList()[0].MovieImg" alt="Card image cap">
                <div class="card-body">
                    <h2 class="card-title">@Model.AllImages.ToList()[0].Title</h2>
                    <p class="card-text">@Model.AllImages.ToList()[0].Description</p>
                    <a href="@Model.AllImages.ToList()[0].MovieURL" class="btn btn-primary">Läs mer &rarr;</a>
                </div>
                <div class="card-footer text-muted">
                    Kategori:
                    <a href="?Category=@Model.AllImages.ToList()[0].Category">@Model.AllImages.ToList()[0].Category</a>
                </div>
            </div>-->
            <!-- Blog Post 2 -->
            <!--<div class="card mb-4">
                <img class="card-img-top" src="~/img/@Model.AllImages.ToList()[1].MovieImg" alt="Card image cap">
                <div class="card-body">
                    <h2 class="card-title">@Model.AllImages.ToList()[1].Title</h2>
                    <p class="card-text">@Model.AllImages.ToList()[1].Description</p>
                    <a href="@Model.AllImages.ToList()[1].MovieURL" class="btn btn-primary">Läs mer &rarr;</a>
                </div>
                <div class="card-footer text-muted">
                    Kategori:
                    <a href="?Category=@Model.AllImages.ToList()[1].Category">@Model.AllImages.ToList()[1].Category</a>
                </div>
            </div>-->
            <!-- Blog Post 3 -->
            <!--<div class="card mb-4">
                <img class="card-img-top" src="~/img/@Model.AllImages.ToList()[2].MovieImg" alt="Card image cap">
                <div class="card-body">
                    <h2 class="card-title">@Model.AllImages.ToList()[2].Title</h2>
                    <p class="card-text">@Model.AllImages.ToList()[2].Description</p>
                    <a href="@Model.AllImages.ToList()[2].MovieURL" class="btn btn-primary">Läs mer &rarr;</a>
                </div>
                <div class="card-footer text-muted">
                    Kategori:
                    <a href="?Category=@Model.AllImages.ToList()[2].Category">@Model.AllImages.ToList()[2].Category</a>
                </div>
            </div>-->
            <!-- Blog Post 4 -->
            <!--<div class="card mb-4">
                <img class="card-img-top" src="~/img/@Model.AllImages.ToList()[3].MovieImg" alt="Card image cap">
                <div class="card-body">
                    <h2 class="card-title">@Model.AllImages.ToList()[3].Title</h2>
                    <p class="card-text">@Model.AllImages.ToList()[3].Description</p>
                    <a href="@Model.AllImages.ToList()[3].MovieURL" class="btn btn-primary">Läs mer &rarr;</a>
                </div>
                <div class="card-footer text-muted">
                    Kategori:
                    <a href="?Category=@Model.AllImages.ToList()[3].Category">@Model.AllImages.ToList()[3].Category</a>
                </div>
            </div>-->
            <!-- Pagination -->
            <ul class="pagination justify-content-center mb-4">
                <li class="page-item">
                    <a class="page-link" href="#">&larr; Äldre</a>
                </li>
                <li class="page-item disabled">
                    <a class="page-link" href="#">Nyare &rarr;</a>
                </li>
            </ul>

        </div>

        <!-- Sidebar Widgets Column -->
        <div class="col-md-4">

            <!-- Search Widget -->
            <div class="card my-4">
                <h5 class="card-header">Sök</h5>
                <div class="card-body">
                    <div class="input-group">
                        <input type="text" class="form-control" placeholder="Sök efter...">
                        <span class="input-group-append">
                            <button class="btn btn-secondary" type="button">Sök!</button>
                        </span>
                    </div>
                </div>
            </div>

            <!-- Categories Widget -->
            <div class="card my-4">
                <h5 class="card-header">Kategorier</h5>
                <div class="card-body">
                    <div class="row">
                        <div class="col-lg-6">
                            <ul class="list-unstyled mb-0">
                                <li>
                                    <a href="?Category=">Alla kategorier</a>
                                </li>
                                <li>
                                    <a href="?Category=Drama">Drama</a>
                                </li>
                                <li>
                                    <a href="?Category=Action">Action</a>
                                </li>
                            </ul>
                        </div>
                        <div class="col-lg-6">
                            <ul class="list-unstyled mb-0">
                                <li>
                                    <a href="?Category=Adventure">Adventure</a>
                                </li>
                                <li>
                                    <a href="?Category=Crime">Crime</a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Side Widget -->
            <div class="card my-4">
                <h5 class="card-header">Alla filmer</h5>
                <div class="container">
                    <div class="row row-cols-auto">
                        @foreach (var image in Model.AllImages)
                        {
                            <div class="col-6"> 
                                <img class="col" src="~/img/@image.MovieImg">
                            </div>
                        }
                    </div>
                </div>

                @*@foreach (var image in Model.AllImages)
                    {
                        <div class="card h-100">
                            <a href="#"><img class="card-img-top" src="~/img/@image.MovieImg" alt=""></a>
                        </div>
                    }*@
            </div>

        </div>

    </div>
    <!-- /.row -->

</div>
<!-- /.container -->
